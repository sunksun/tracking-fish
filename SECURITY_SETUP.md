# 🔐 คู่มือการตั้งค่าความปลอดภัยสำหรับ Firebase

## ⚠️ สำคัญมาก: API Key ถูกเปิดเผยบน GitHub

API Key ของคุณถูกเปิดเผยบน GitHub แล้ว คุณจะต้อง:
1. สร้าง API Key ใหม่ใน Firebase Console
2. ตั้งค่า Environment Variables ในโปรเจค

---

## ขั้นตอนที่ 1: สร้าง API Key ใหม่ใน Firebase

### 1.1 ไปที่ Google Cloud Console
1. เปิด https://console.cloud.google.com/
2. เลือกโปรเจค `tracking-fish-app`
3. ไปที่เมนู "APIs & Services" > "Credentials"

### 1.2 ลบ API Key เก่า (แนะนำ)
1. ค้นหา API Key ที่ขึ้นต้นด้วย `AIzaSyBqDCZidQSSGzhZu0hS1bZtxD4pJLYvIgY`
2. คลิก "Delete" เพื่อลบ API Key เก่า (เพราะถูกเปิดเผยแล้ว)
3. ยืนยันการลบ

### 1.3 สร้าง API Key ใหม่
1. คลิกปุ่ม "+ CREATE CREDENTIALS" > "API key"
2. **คัดลอก API Key ใหม่ทันที** (เก็บไว้ใช้ในขั้นตอนถัดไป)
3. คลิก "Edit API key" เพื่อตั้งค่าความปลอดภัย

### 1.4 ตั้งค่าข้อจำกัดสำหรับ API Key (แนะนำเพื่อความปลอดภัย)

#### สำหรับ iOS:
- ในส่วน "Application restrictions" เลือก "iOS apps"
- เพิ่ม Bundle ID: `com.trackingfish.app`

#### สำหรับ Android:
- ในส่วน "Application restrictions" เลือก "Android apps"
- เพิ่ม Package name: `com.trackingfish.app`
- เพิ่ม SHA-1 certificate fingerprint (ดูวิธีหาได้จากคำสั่งด้านล่าง)

#### จำกัดการเข้าถึง API:
ในส่วน "API restrictions" เลือก "Restrict key" และเลือกเฉพาะ:
- ✅ Firebase Authentication API
- ✅ Cloud Firestore API
- ✅ Firebase Storage API
- ✅ Geocoding API (ถ้าใช้)

คลิก "Save" เพื่อบันทึกการตั้งค่า

---

## ขั้นตอนที่ 2: ตั้งค่า Environment Variables

### 2.1 สร้างไฟล์ .env
ในโฟลเดอร์ root ของโปรเจค ให้รันคำสั่ง:

```bash
cp .env.example .env
```

หรือคัดลอกไฟล์ `.env.example` แล้วเปลี่ยนชื่อเป็น `.env`

### 2.2 กรอกข้อมูล Firebase ในไฟล์ .env

เปิดไฟล์ `.env` ด้วย Text Editor และกรอกข้อมูลดังนี้:

```env
# Firebase Configuration
FIREBASE_API_KEY=<ใส่ API_KEY ใหม่ที่คัดลอกจากขั้นตอนที่ 1.3>
FIREBASE_AUTH_DOMAIN=tracking-fish-app.firebaseapp.com
FIREBASE_PROJECT_ID=tracking-fish-app
FIREBASE_STORAGE_BUCKET=tracking-fish-app.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=587580376587
FIREBASE_APP_ID=1:587580376587:web:a35c9caf6acab6a110290e
```

**ตัวอย่างที่กรอกแล้ว:**
```env
FIREBASE_API_KEY=AIzaSyDabcdef123456789xyz
FIREBASE_AUTH_DOMAIN=tracking-fish-app.firebaseapp.com
FIREBASE_PROJECT_ID=tracking-fish-app
FIREBASE_STORAGE_BUCKET=tracking-fish-app.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=587580376587
FIREBASE_APP_ID=1:587580376587:web:a35c9caf6acab6a110290e
```

### 2.3 หาข้อมูล Firebase Configuration (ถ้าไม่แน่ใจ)

1. เปิด https://console.firebase.google.com/
2. เลือกโปรเจค `tracking-fish-app`
3. คลิกไอคอนเฟือง ⚙️ (Project Settings)
4. เลื่อนลงไปหาส่วน "Your apps"
5. เลือก Web app (ถ้ายังไม่มีให้สร้างใหม่)
6. คัดลอกค่าจาก `firebaseConfig` มาใส่ในไฟล์ `.env`

---

## ขั้นตอนที่ 3: ทดสอบแอป

### 3.1 ล้าง Cache และ Restart

```bash
# หยุด Expo server ที่กำลังรันอยู่ (กด Ctrl+C)

# ล้าง cache และเริ่มใหม่
npm start -- --reset-cache
```

### 3.2 ทดสอบการเข้าสู่ระบบ

1. เปิดแอปบน Emulator หรือ Physical Device
2. ลองเข้าสู่ระบบด้วยเบอร์โทรศัพท์
3. ตรวจสอบว่าระบบทำงานปกติ
4. ตรวจสอบว่าข้อมูลบันทึกขึ้น Firebase ได้

---

## ⚠️ สิ่งที่ต้องระวัง - สำคัญมาก!

### ❌ **อย่า**ทำ

- ❌ **อย่า commit ไฟล์ `.env` ขึ้น GitHub**
  - ไฟล์นี้ถูก ignore แล้วใน `.gitignore`
  - ตรวจสอบด้วย `git status` ก่อน commit ทุกครั้ง

- ❌ **อย่า hardcode API Key ในโค้ด**
  - ใช้ environment variables เท่านั้น
  - ไฟล์ `src/config/firebase.js` ถูกแก้ไขให้ใช้ Constants แล้ว

- ❌ **อย่าแชร์ไฟล์ `.env` ให้ใคร**
  - ไม่ทาง Email, Chat, หรือ Cloud Storage
  - แต่ละคนต้องสร้างของตัวเอง

- ❌ **อย่าเอา API Key ใส่ใน Screenshot**
  - ระวังตอนแชร์หน้าจอหรือสอนคนอื่น

### ✅ **ควร**ทำ

- ✅ เก็บไฟล์ `.env` ไว้ในเครื่องของคุณเท่านั้น
- ✅ ถ้ามีทีมงาน ให้แต่ละคนสร้าง `.env` จาก `.env.example`
- ✅ ตั้งค่า API restrictions ใน Google Cloud Console
- ✅ ใช้ `.env.example` เป็น template (ไม่มีค่าจริง)
- ✅ สำรองข้อมูล Firebase config ไว้ในที่ปลอดภัย (เช่น Password Manager)

---

## 🔍 ตรวจสอบความปลอดภัย

### ตรวจสอบว่าไฟล์ .env ถูก Ignore หรือไม่

```bash
git status
```

**ผลลัพธ์ที่ถูกต้อง:** ไม่ควรเห็นไฟล์ `.env` ในรายการ

**ถ้าเห็น `.env` ในรายการ:**
1. เช็ค `.gitignore` ว่ามีบรรทัดนี้หรือไม่:
   ```
   .env
   .env.local
   .env*.local
   ```
2. ถ้ามีแล้วแต่ยังเห็น ให้รัน:
   ```bash
   git rm --cached .env
   ```

### ตรวจสอบว่าโค้ดไม่มี Hardcoded Secrets

```bash
# ค้นหา API Key patterns ในโค้ด
git grep -i "AIza"
git grep -i "apikey.*:" src/
```

**ผลลัพธ์ที่ถูกต้อง:** ไม่ควรพบค่า API Key จริงๆ ในโค้ด

---

## 📝 สรุปการเปลี่ยนแปลง

ตอนนี้โปรเจคของคุณมีความปลอดภัยแล้ว:

### การเปลี่ยนแปลงที่ทำไปแล้ว:

1. ✅ **src/config/firebase.js**
   - ลบ API Key ที่ hardcode
   - ใช้ `Constants.expoConfig.extra` อ่านค่าจาก environment

2. ✅ **app.config.js**
   - เพิ่ม `import 'dotenv/config'`
   - เพิ่มส่วน `extra` สำหรับส่งค่า environment ให้แอป

3. ✅ **.gitignore**
   - เพิ่มการ ignore ไฟล์ `.env`
   - ป้องกันไม่ให้ secrets ขึ้น Git

4. ✅ **.env.example**
   - Template สำหรับทีมงาน
   - ไม่มีค่าจริง

### ไฟล์ที่คุณต้องสร้างเอง:

- 📄 `.env` - สร้างจาก `.env.example` และกรอกค่าจริง

---

## 🚀 พร้อมแล้ว!

คุณพร้อมที่จะ commit และ push โค้ดขึ้น GitHub โดยไม่เปิดเผย API Key แล้ว!

```bash
git add .
git commit -m "Security: Move Firebase credentials to environment variables"
git push origin main
```

---

## 📞 ข้อมูลเพิ่มเติม

### หา SHA-1 Fingerprint สำหรับ Android

```bash
# Development (Debug) certificate
cd android
./gradlew signingReport

# หรือใช้ keytool
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
```

### แหล่งข้อมูลอ้างอิง

- [Firebase Security Best Practices](https://firebase.google.com/docs/rules/basics)
- [Expo Environment Variables](https://docs.expo.dev/guides/environment-variables/)
- [Securing API Keys](https://cloud.google.com/docs/authentication/api-keys)

---

## ❓ FAQ

**Q: API Key ของ Firebase ไม่ต้องเป็นความลับหรือ?**
A: Firebase API Key ต้องการการป้องกันระดับหนึ่ง แม้จะไม่เป็นความลับเหมือน Server API Key แต่ควร:
- ตั้งค่า Application restrictions
- ตั้งค่า Firestore Security Rules
- ตั้งค่า Storage Security Rules
- จำกัด API ที่ใช้ได้

**Q: ต้องลบและสร้าง Repository ใหม่ไหม?**
A: ไม่จำเป็น ถ้าทำตามขั้นตอนนี้ และ regenerate API Key ใหม่ ก็ปลอดภัยแล้ว

**Q: ทำไมต้องใช้ .env แทนการ hardcode?**
A:
- ป้องกันการเปิดเผยข้อมูลบน GitHub
- แยก configuration ตาม environment (dev/staging/prod)
- ทีมงานแต่ละคนใช้ config ของตัวเองได้
- เป็น best practice ที่ยอมรับกันทั่วไป
